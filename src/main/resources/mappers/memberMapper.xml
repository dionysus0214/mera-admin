<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mera.mapper.MemberMapper">

    <resultMap type="org.mera.domain.MemberVO" id="memberMap">
        <id property="userid" column="uid"/>
        <result property="userid" column="uid"/>
        <result property="userpw" column="upw"/>
        <result property="userName" column="uname"/>
        <result property="use_yn" column="use_yn"/>
<!--        <result property="reg_dt" column="reg_dt"/>-->
<!--        <result property="upd_dt" column="upd_dt"/>-->

        <collection property="authList" resultMap="authMap">
        </collection>
    </resultMap>

    <resultMap type="org.mera.domain.AuthVO" id="authMap">
        <result property="userid" column="uid"/>
        <result property="auth" column="auth"/>
    </resultMap>

    <select id="login" resultMap="memberMap">
        <![CDATA[
        SELECT ma.uid, ma.upw, ma.uname, ma.use_yn, auth
        FROM mera_admuser ma
            LEFT OUTER JOIN mera_admuser_auth auth
                ON ma.uid = auth.uid
        WHERE ma.uid = #{userName}
        ]]>
    </select>
</mapper>
